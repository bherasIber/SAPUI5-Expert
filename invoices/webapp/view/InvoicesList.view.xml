<mvc:View id="InvoicesListView" 
        controllerName="logaligroup.invoices.controller.InvoicesList"
        xmlns="sap.m"
        xmlns:mvc="sap.ui.core.mvc">
    <List id="invoiceList" 
        headerText="{i18n>invoicelistTitle}" 
        class="sapUiResponsiveMargin" 
        width="auto" 
        items="{ path: 'northwind>/Invoices',
                 sorter: { path: 'ShipperName',
                           group: true}
                 
                }">
        <headerToolbar>
            <Toolbar id="toolList">
                <Title id="toolTitle" text="{i18n>invoicelistTitle}"></Title>
                <ToolbarSpacer id="spaceList"/>
                <SearchField id="searchList" width="50%" search=".onFilterInvoices"/>
            </Toolbar>
        </headerToolbar>
        <items>
            <ObjectListItem id="objListItem" title="{northwind>Quantity} x {northwind>ProductName}" 
                                            number="{ parts: [{path:'northwind>ExtendedPrice'}]}"
                                            numberUnit="{currency>/usd}"
                                            numberState="{= ${northwind>ExtendedPrice} > 50 ? 'Success' : 'Error'}"
                                            type="Navigation"
                                            press="navigateToDetails">
                <!--firstStatus>
                    <ObjectStatus id="objStatus" text="{ path: 'invoices>Status',
                                                        formatter: '.formatter.invoiceStatus'}"></ObjectStatus>
                </firstStatus-->
            </ObjectListItem>
        </items>
    </List>
</mvc:View>